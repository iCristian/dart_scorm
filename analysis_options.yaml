# Reglas de análisis para el paquete `scorm`
# Basado en las recomendaciones oficiales de Dart
include: package:lints/recommended.yaml

linter:
  rules:
    always_declare_return_types: true
    avoid_print: true

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  exclude:
    - example/**
